[Unit]
# Second bot, beside claudecoach-bot.service, not replacing it. Own token, own
# process, own log. Install with:
#   cp ClaudeCoach/telegram/claudecoach-nutrition.service /etc/systemd/system/
#   systemctl daemon-reload && systemctl enable --now claudecoach-nutrition
Description=ClaudeCoach nutrition logger (Telegram)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/diamondpeak-site
ExecStart=/usr/bin/python3 /root/diamondpeak-site/ClaudeCoach/telegram/nutrition_bot.py
# The ladder's LLM rung shells out to the claude CLI, which needs the same auth the
# coach bot uses. cc.env holds the setup token; credentials.json is NOT the file.
EnvironmentFile=-/root/cc.env
Environment=CLAUDE_BIN=/usr/bin/claude
Restart=always
RestartSec=10
# A crash loop must not spin: five restarts in a minute and it stays down so the
# failure is visible rather than hidden behind a bot that appears to be running.
StartLimitBurst=5
StartLimitIntervalSec=60
StandardOutput=append:/root/diamondpeak-site/ClaudeCoach/telegram/nutrition_bot.log
StandardError=append:/root/diamondpeak-site/ClaudeCoach/telegram/nutrition_bot.log

[Install]
WantedBy=multi-user.target
