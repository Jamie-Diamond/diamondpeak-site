# ClaudeCoach handover — 17 Aug 2026, ~11:30 BST

Read `~/.claude/projects/-Users-diamondpeakconsulting/memory/MEMORY.md` first, especially
`project_claudecoach_bug_register.md`. Standing rules that bit us today are in there.

## Non-negotiables (Jamie's own rules, learned the hard way)

- **Read the RAW logs**, never curated stores. Coach: `telegram/bot.log`. Nutrition:
  `telegram/nutrition_bot.log`. Cron logs: `/root/Library/Logs/ClaudeCoach/` (NOT in the repo).
- **His bug register is `athletes/jamie/feedback-log.json`** — 77 records, ~24 open. Read it
  before asking him what to fix.
- **Verified means verified where it runs.** Push, pull on the VM, restart, then prove it live.
- **Explicit-path `git add` only.** Never `git add -A` while agents are active.
- **VM SSH is flaky**: retry loops with 40s gaps. `set -a && . /root/.claude/cc.env && set +a`
  before any VM `claude` CLI call. VM repo: `/Users/diamondpeakconsulting/diamondpeak-site`.
- Athlete-stated figures are law. No em-dashes. UK English.

## Shipped and verified live today

| Commit | What |
|---|---|
| `fbae6e57` | Legacy `sodium_mg` counts again (22 records migrated); post-session fuel capture no longer rides-only |
| `e7ea5bd2` | Rule-overflow spill to feedback log + honest reply when a rule is refused; `unclear` corrections ASK (structural, cannot re-resolve); junk exclusions blocked; gate timeout 20s→45s; stale callbacks quiet in BOTH transports |
| `eb5b2dbe` | plan_audit hard fails Telegram Jamie, deduped by finding identity. Live-proven: one alert sent, second run silent, `rc=1` intact |
| `9a1b7f08` | `lib/rest_day.py` reserves the rest day at build time; recorded train-through waiver now actually reaches the validator |

Data repaired by hand on the VM (backups alongside, suffix `.bak-2026-08-17-repair`):
Edge-830 elevation rule recovered (142 tokens, under the 150 cap) + evidence in feedback log;
both half-cookies repriced 97/120 → 146 kcal from the photographed label; junk exclusions
`['logged']` (17 Aug) and `['be logged as supplements']` (15 Aug) cleared; nutrition republished.

## Rest-day reserver: VERIFIED LIVE (dry run, nothing pushed)

Dry run of Kathryn's week of 2026-08-17 (`scratchpad/shadow-17aug.sh`, no `--push`):
reserved a rest day, `no_rest_day` no longer fails, ZERO push attempts. Note the first dry
run (week of 24 Aug) did NOT exercise the reserver at all — that week already had a rest
day so `reserve()` returned early. Use a genuinely 7/7 week to test this path.

It reserved **Wed 19 Aug and escalated rather than silently dropping a key session**:
"Wed 2026-08-19 carried a KEY session and was still the only day available - every cheaper
day is agreed with the athlete. Worth asking them which day they would rather rest."
295 load came off and was offered back to easy endurance within caps. This is the designed
behaviour for the all-cheaper-days-pinned case, and it is why Kathryn must be ASKED.

Careful with the two different answers: the dry run REBUILDS the week, so its shape differs
from the live calendar. In the rebuild Wednesday was the cheapest unpinned day; on her
CURRENT calendar it is Thursday's 29-load easy run, which is what she was asked about.

## Open, with Jamie

1. **Kathryn was asked (11:0x) whether to make Thursday 20 Aug her rest day** and has not
   replied. Her week is 7/7 loaded, last rest day 13 Aug, ten consecutive days by Sunday.
   Do NOT change her calendar without her answer — Jamie's instruction was "fix the system,
   probably Thursday's run, but also ask her".
2. **Both next weeks are EMPTY, and that was an ARTEFACT, not a defect.** plan_audit walks
   current + next week every morning; `scripts/weekly-plan.sh` builds only the coming week,
   Sunday 18:00. So next week is legitimately empty all week and the armed floor scored it
   "0 TSS ... DETRAINS the athlete" every run, for every athlete (the week of 17 Aug
   collected 16 such flags and is now fully planned). Since `eb5b2dbe` that pages Jamie.
   FIXED: `lib/plan_audit.py` no longer arms `weekly_tss_floor` on a FUTURE week with zero
   planned sessions - it emits a "NOT GENERATED YET" note instead (log/JSON only, not in
   `fails`, so it neither blocks nor alerts). An empty CURRENT week still hard-fails, and a
   future week built under the floor still hard-fails. Tests:
   `ironman-analysis/tests/test_plan_audit_unbuilt_week.py`. NB the earlier note here that
   Kathryn's 24 Aug "may be a `manual_easy_weeks` entry" was wrong: she has no such key
   (only `deload_skip_weeks: ["2026-08-17"]`); Jamie is the only athlete with a manual easy
   week, for the 20 Jul Dorney taper. The "313 TSS target vs a 505 floor" figure is
   UNVERIFIED and looks arithmetically impossible (`weekly_tss_floor = min(rec,
   maintenance)`); re-derive it from a live `required_tss` before acting on it.
3. **Jamie's `day_rules` look stale** — config forbids Thu rides, Wed swims, Fri runs, yet
   those are exactly what he agreed and rides weekly. Three hard fails on his live week are
   all his own agreed sessions. Decision pending: update config, or have the bot push back
   when he asks for a forbidden day. Same pattern hit Kathryn (her Sunday swim was her own
   08:25 request, executed without flagging the rule).
4. **Coach bot latency is the worst live problem and is NOT started** (task #18). Measured
   17 Aug: time-to-first-text 128s, 125s, 233s, 427s; one turn totalled 526s; new sessions
   send 111–117KB prompts. Needs a measurement-led design pass; Jamie must schedule it.

## Open tasks worth knowing

- **#30** Abort an in-flight reply + scope confirm before a whole-week replan (Jamie's own
  bug, 16 Aug: Kathryn said "2.5 hours" meaning one day, bot replanned the week).
- **#31** `stated_macros` requires kcal, so "with 21g protein" is not honoured as his figure.
  Path fixed 17 Aug, figure not. Fix is in `lib/nutrition_resolve.py`.
- **#32** PRE-EXISTING failure (verified in a clean worktree at `e7ea5bd2`, not ours):
  `test_day_overrides_capture.py::...dispatch_ORDER` —
  `looks_like_action_instruction("move the swim to Wednesday")` returns True, test expects
  False. Capture-swallowing class; same shape as Kathryn's swim-move message.
- **#19** Exclusion over-reach: items (1) and (3) look done in the tree; item (2)
  verify-live-routing from the RAW log still needs doing and needs VM access.
- **#I** Callback acks still fire AFTER the work (100–500s). The 400 error is silenced but the
  real fix is acking on receipt — needs call sites moved in both bot files.
- **#J** Photo plates log RAW CoFID rows ("Bacon rashers, streaky, raw" off a cooked plate).
  Check whether the photo path bypasses the cooked-row preference before patching.
- **#K** A claimed ICU write never landed (16 Aug 21:56, "still absent after retry").

## Traps

- **150-token rule cap** (`RULE_MAX_TOKENS`, `BYTES_PER_TOKEN=2.60` in `scripts/bug-fixer.py`)
  → a `[perm]` rule must be under ~390 bytes. `session-sync.py` re-enforces HOURLY, so a
  hand-written over-cap rule gets culled later too.
- **Two bots, two Telegram transports.** `nutrition_bot.py` imports `lib/tg.py`; `bot.py` has
  its own `tg_post`. A transport fix in one does not reach the other.
- **Sub-agents went idle without reporting** three times today. Verify their work by reading
  the code and running it yourself; do not trust the summary. One agent's report contradicted
  its own shipped code (said the guard returned True; it actually returns None).
- **Do not commit while an agent is writing.** Check mtimes; wait for ~90s of quiet.
- The permission classifier intermittently blocks VM writes to live athlete data. Retry via
  Write-then-`zsh script`; if it blocks twice, surface it to Jamie rather than working around.
